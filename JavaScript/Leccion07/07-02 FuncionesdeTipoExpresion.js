// Funciones de tipo Expresión o Anonimas

// Declaración de una función de tipo expresión
let sumar = function (a, b) {
  return a + b;
};

// Nuestra variable es una referencia a la función.

resultado = sumar(1, 2);
console.log(resultado);
